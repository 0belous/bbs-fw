<UserControl x:Class="BBSFW.View.ThrottleResponseView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:BBSFW.View"
             xmlns:oxy="http://oxyplot.org/wpf"
             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="10">
			<RadioButton Content="Linear" Margin="5" IsChecked="{Binding IsLinearChecked}" Grid.Row="0" VerticalAlignment="Center" />
			<RadioButton Content="Quadratic" Margin="5" IsChecked="{Binding IsQuadraticChecked}" Grid.Row="1" VerticalAlignment="Center" />
			<Label Content="Quadratic Factor" Margin="0,-1,0,0" Grid.Row="0" VerticalAlignment="Center" />
			<TextBox Text="{Binding ConfigVm.QuadraticFactor,  UpdateSourceTrigger=LostFocus}" Padding="2" Grid.Row="2" VerticalAlignment="Center" IsEnabled="{Binding IsQuadraticChecked}" MinWidth="50" />
		</StackPanel>

        <oxy:PlotView Grid.Row="1" Model="{Binding PlotModel}" />
    </Grid>
</UserControl>
